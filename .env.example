# Адрес бэкенда (backend/). Скопируйте в .env.local при необходимости.
# Бэкенд должен разрешать origin админки в ADMIN_ORIGIN (по умолчанию http://localhost:8443).
VITE_API_URL=http://localhost:8080
